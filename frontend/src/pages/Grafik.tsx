import React from 'react';
import { Box, Typography } from '@mui/material';

const Grafik: React.FC = () => {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h4">
        Grafik
      </Typography>
      {/* Halaman grafik akan diisi nanti */}
    </Box>
  );
};

export default Grafik; 